query getAllPostsForBlogPage($locale: Locale!) {
  posts(orderBy: published_DESC, locales: [$locale]) {
    id
    title
    slug
    published
    cover(locales: [$locale]) {
      handle
      height
      width
    }
    seo(locales: [$locale]) {
      description
    }
  }
}
