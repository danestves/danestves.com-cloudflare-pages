import React from "react"

export default ({ children }) => (
  <blockquote className="border-l-8 pl-6 italic mb-6 border-gray-400 text-gray-500 dark:text-gray-700 dark:border-gray-700">
    {children}
  </blockquote>
)
